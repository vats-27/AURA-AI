# AuraAI Backend dependencies — pinned to the exact versions verified working locally
# and on Render (Python 3.12). Direct deps are pinned with == to prevent version drift
# on fresh installs. Transitive/compiled deps are left to pip so it picks the correct
# platform wheels (Linux/Windows) at install time.
#
# NOTE: composio 0.8.x is incompatible with composio-client >= 1.4x
# (they removed composio_client.types.tool_router_create_session_params),
# so composio-client is pinned to 1.33.0.

fastapi==0.119.1
uvicorn[standard]==0.34.3
motor==3.7.1
pymongo==4.17.0
python-dotenv==1.2.2
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
google-auth==2.49.2
python-multipart==0.0.26
pydantic[email]==2.13.3
langchain==0.3.28
langchain-google-genai==2.1.12
langchain-openai==0.3.35
langchain-community==0.3.31
langchain-text-splitters==0.3.11
docx2txt==0.9
pypdf==6.10.2
python-docx==1.2.0
composio==0.8.20
composio-langchain==0.8.20
composio-client==1.33.0
sentry-sdk==2.58.0
inflection==0.5.1
jsonref==1.1.0
semver==3.0.4
paramiko==4.0.0
rich==15.0.0
pytz==2026.1.post1
requests==2.33.1
httpx==0.28.1
